资源收发服务器ResourceReceiverServer：
    1.接收者；
    2.先确定接收文件集合以及发送端数量；
    3.建立服务器
    4.开始侦听发送端；
    5.ResourceReceiver开始接收；
    6.先接收头部，包含文件id、偏移量、长度；
    7.根据头部确定接收的文件信息，接收相应长度的内容，即确定一个资源块；
    8.资源块写入本地；
    9.重复10 直到接收该发送端完毕；
    10.重复4 直到接收完毕；
    11.善后工作，若发现有未传部分，则断点续传。
资源发送者中心ResourceSenderCenter:
    1.发送者；
    2.先确定发送文件集合以及需要发送文件内容列表；
    3.建立Socket；
    4.ResourceSender开始发送；
    5.根据发送文件内容列表发送；
    7.先发送头部，再发送真正内容；
    8.重复7 直到发送完毕；


